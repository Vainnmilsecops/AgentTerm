{
  "name": "@agentterm/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "AgentTerm read-only MCP server exposing 4 JSON-RPC tools.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agentterm/application": "workspace:*",
    "@agentterm/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "tsup": "8.5.1"
  }
}